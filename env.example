# Database Connection
# Connect to your NeonDB or PostgreSQL database
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# NextAuth Configuration
# Generate a secret with `openssl rand -base64 32` or similar
NEXTAUTH_SECRET="your-super-secret-key-change-me"
NEXTAUTH_URL="http://localhost:3000"

# Optional: NextAuth Providers (if you add Google, GitHub, etc. later)
# GITHUB_ID=""
# GITHUB_SECRET=""
